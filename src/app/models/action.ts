export enum Action {
    move = 'Move to an activity',
    mineFoo = 'Mine `foo`',
    mineBar = 'Mine `bar`',
    buildFoobar = 'Build a `foobar`',
    sell = 'Sell `foobar`',
    buy = 'Buy a robot'
}
